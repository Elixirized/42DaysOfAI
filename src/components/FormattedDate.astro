---
interface Props {
	date?: Date;
}

const { date } = Astro.props as Props;
---
{date ? (

	<time datetime={date.toISOString()}>
		{
			date.toLocaleDateString('en-us', {
				year: 'numeric',
				month: 'short',
				day: 'numeric',
			})
		}
	</time>
) : null}
